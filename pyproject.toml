[project]
name = "neilyst"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.13.2",
]

[tool.ruff]
select = ["F", "E", "W", "I", "N", "UP", "B", "C4"]
ignore = ["E203", "E501", "F841", "F401"]  # 把未使用交给 Pylance
